#include<stdio.h>

int main(){
    int length;
    int breadth;
    int area;
    printf("Enter the length of the triangle:\n");
    scanf("%d",&length);
    printf("Enter the breadth of the triangle:\n");
    scanf("%d",&breadth);
    printf("The area of the triangle is %d",area=length*breadth);
    return 0;
}